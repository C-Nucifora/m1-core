[workspace]
members = ["xtask"]

[package]
name = "m1-core"
version = "0.2.0"
edition = "2024"
description = "Shared CST helpers, symbol model, and diagnostics for the MoTeC M1 script language"
publish = false

[dependencies]
tree-sitter = "0.25"
tree-sitter-m1 = { path = "vendor/tree-sitter-m1" }
